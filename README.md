# RestAPI
This is a RestAPI for tasks CRUD. It allows to GET, POST, PUT, and DELETE. To use it type "uvocorn main:app --reload". Required packages can be installed with "pip install fastapi uvicorn".
